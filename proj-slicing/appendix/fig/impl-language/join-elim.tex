\begin{figure}
\shadebox{$\sigma \join \sigma'$}
\begin{salign}
   \bot \join \sigma &= \sigma
   \\
   \sigma \join \bot &= \sigma
   \\
   (\elimVar{x}{\kappa}) \join (\elimVar{x}{\kappa'}) &= \elimVar{x}{\kappa \join \kappa'}
   \\
\end{salign}
\caption{Join of eliminator slices}
\end{figure}
