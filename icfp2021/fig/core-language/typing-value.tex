\begin{figure}[H]
\flushleft \shadebox{$\vdash \rho: \Gamma$}
\begin{smathpar}
\inferrule*
{
   \vdash v_i: A_i
   \quad
   (\forall i \numleq n)
}
{
   \vdash \seqRange{\bind{z_1}{v_1}}{\bind{z_n}{v_n}}: \seqRange{\bind{z_1}{A_1}}{\bind{z_n}{A_n}}
}
\end{smathpar}
\\[2mm]
\flushleft \shadebox{$\Gamma \vdash h: \Delta$}
\begin{smathpar}
   \inferrule*[right={
      \textnormal{$\Delta = \seqRange{\bind{x_1}{\tyFun{A_1}{B_1}}}{\bind{x_n}{\tyFun{A_n}{B_n}}}$}
   }]
   {
      \Gamma \concat \Delta \vdash \sigma_i: \tyFun{A_i}{B_i}
      \quad
      (\forall i \numleq n)
   }
   {
      \Gamma \vdash \seqRange{\bind{x_1}{\sigma_1}}{\bind{x_n}{\sigma_n}}: \Delta
   }
\end{smathpar}
\\[2mm]
\flushleft \shadebox{$\vdash v: A$}
\begin{smathpar}
\inferrule*
{
   \strut
}
{
   \vdash \exInt{n}: \tyInt
}
%
\and
%
\inferrule*[
   right={$\phi \in \Int \to \Int$}
]
{
   \strut
}
{
   \vdash \exPrim{\phi}: \tyFun{\tyInt}{\tyInt}
}
%
\and
%
\inferrule*[
   right={$\phi \in \Int \to \Int \to \Int$}
]
{
   \strut
}
{
   \vdash \exPrim{\phi}: \tyFun{\tyInt}{\tyFun{\tyInt}{\tyInt}}
}
%
\and
%
\inferrule*
{
   \strut
}
{
   \vdash \exTrue: \tyBool
}
%
\and
%
\inferrule*
{
   \strut
}
{
   \vdash \exFalse: \tyBool
}
%
\and
%
\inferrule*
{
   \vdash u: A
   \\
   \vdash v: B
}
{
   \vdash \exPair{u}{v}: \tyProd{A}{B}
}
%
\and
%
\inferrule*
{
   \vdash \rho: \Gamma
   \\
   \Gamma \vdash h: \Delta
   \\
   \Gamma \concat \Delta \vdash \sigma: \tyFun{A}{B}
}
{
   \vdash \exClosureRec{\Gamma}{\rho}{h}{\sigma}: \tyFun{A}{B}
}
%
\and
%
\inferrule*
{
   \strut
}
{
   \vdash \exNil: \tyList{A}
}
%
\and
%
\inferrule*
{
   \vdash u: A
   \\
   \vdash v: \tyList{A}
}
{
   \vdash (\exCons{u}{v}): \tyList{A}
}
%
\and
%
\inferrule*
{
   \vdash v_{i,j}: A
   \quad
   (\forall (i,j) \leq (\ihat,\jhat))
}
{
   \vdash \exMatrix{v_{i,j}}{i}{j}{\exPair{\ihat}{\jhat}}: \tyMatrix{A}
}
\end{smathpar}
\caption{Typing rules for environments and values}
\end{figure}
