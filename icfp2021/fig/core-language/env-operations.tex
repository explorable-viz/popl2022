\begin{figure}[H]
\flushleft \shadebox{$\hole_{\Gamma}$}
\begin{salign}
   \hole_{\seqRange{\bind{z_1}{A_1}}{\bind{z_n}{A_n}}} &= \seqRange{\bind{z_1}{\hole}}{\bind{z_n}{\hole}}
\end{salign}
\\[2mm]

\flushleft \shadebox{$\envLookup{\rho}{z}{v}$}
\begin{smathpar}
   \inferrule*
   {
      \strut
   }
   {
      \envLookup{(\rho \concat \bind{z}{v})}{z}{v}
   }
   %
   \and
   %
   \inferrule*[
      right={$z' \neq z$}
   ]
   {
      \envLookup{\rho}{z}{v}
   }
   {
      \envLookup{(\rho \concat \bind{z'}{u})}{z}{v}
   }
\end{smathpar}
\\[2mm]

\flushleft \shadebox{$\envLookupBwd{\rho}{\Gamma}{\bind{z}{v}}$}
\begin{smathpar}
   \inferrule*
   {
      \strut
   }
   {
      \envLookupBwd{\hole_{\Gamma} \concat \bind{z}{u}}{\Gamma \concat \bind{z}{A}}{\bind{z}{u}}
   }
   %
   \and
   %
   \inferrule*[
      right={$z \neq z'$}
   ]
   {
      \envLookupBwd{\rho}{\Gamma}{\bind{z}{v}}
   }
   {
      \envLookupBwd{\rho \concat \bind{z'}{\hole}}{\Gamma \concat \bind{z'}{A}}{\bind{z}{v}}
   }
\end{smathpar}
\caption{Hole environment, environment lookup and environment update}
\end{figure}
