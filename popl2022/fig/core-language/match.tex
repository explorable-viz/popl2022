\begin{figure}[H]
\flushleft \shadebox{$\explVal{w}{v, \sigma \matchR \rho, \kappa}$}
\begin{smathpar}
   \inferrule*[lab={\ruleName{$\matchR$-var}}]
   {
      \strut
   }
   {
      \explVal{\matchVar{x}}{v, \elimVar{x}{\kappa} \matchR \bind{x}{v}, \kappa}
   }
   %
   \and
   %
   \inferrule*[lab={\ruleName{$\matchR$-true}}]
   {
      \strut
   }
   {
      \explVal{\matchTrue}{\exTrue, \elimBool{\kappa}{\kappa'} \matchR \seqEmpty, \kappa}
   }
   %
   \and
   %
   \inferrule*[lab={\ruleName{$\matchR$-false}}]
   {
      \strut
   }
   {
      \explVal{\matchFalse}{\exFalse, \elimBool{\kappa}{\kappa'} \matchR \seqEmpty, \kappa'}
   }
   %
   \and
   %
   \inferrule*[lab={\ruleName{$\matchR$-pair}}]
   {
      \explVal{w}{v, \sigma \matchR \rho, \tau}
      \\
      \explVal{w'}{v', \tau \matchR, \rho', \kappa}
   }
   {
      \explVal{\matchPair{w}{w'}}{\exPair{v}{v'}, \elimProd{\sigma} \matchR \rho \concat \rho', \kappa}
   }
   %
   \and
   %
   \inferrule*[
      lab={\ruleName{$\matchR$-record-empty}}
   ]
   {
      \strut
   }
   {
      \explVal{\exRec{\seqEmpty}}
              {\seqEmpty, \elimRec{\seqEmpty}{\kappa} \matchR \seqEmpty, \kappa}
   }
   %
   \and
   %
   \inferrule*[
      lab={\ruleName{$\matchR$-record}}
   ]
   {
      \explVal{w'}{v, \sigma \matchR \rho, \sigma'}
      \\
      \explVal{\vec{\bind{y}{w}}}
              {\exRec{\vec{\bind{y}{u}}}, \sigma' \matchR \rho', \kappa}
   }
   {
      \explVal{\exRec{\bind{x}{w'} \concat \vec{\bind{y}{w}}}}
              {\exRec{\bind{x}{v} \concat \vec{\bind{y}{u}}},
               \elimRec{x \concat \vec{y}}{\sigma}
               \matchR
               \rho \concat \rho',
               \kappa}
   }
   %
   \and
   %
   \inferrule*[lab={\ruleName{$\matchR$-nil}}]
   {
      \strut
   }
   {
      \explVal
         {\matchNil}
         {\exNil, \elimList{\kappa}{\sigma} \matchR \seqEmpty, \kappa}
   }
   %
   \and
   %
   \inferrule*[lab={\ruleName{$\matchR$-cons}}]
   {
      \explVal{w}{v, \sigma \matchR \rho, \tau}
      \\
      \explVal{w'}{v', \tau \matchR \rho', \kappa'}
   }
   {
      \explVal
         {(\matchCons{w}{w'})}
         {\exCons{v}{v'}, \elimList{\kappa}{\sigma}
         \matchR
         \rho \concat \rho', \kappa'}
   }
\end{smathpar}
\caption{Pattern-matching}
\end{figure}
