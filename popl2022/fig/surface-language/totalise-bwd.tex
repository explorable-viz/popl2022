\begin{figure}[H]
\flushleft \shadebox{$\totalisebwd{\kappa}{\vec{\pi}}{\kappa'}{\alpha}$}
\begin{smathpar}
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-empty}}
]
{
   \strut
}
{
   \totalisebwd{\kappa}{\seqEmpty}{\kappa}{\FF}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-var}}
]
{
   \sigma \geq \elimVar{x}{\kappa}
   \\
   \totalisebwd{\kappa}{\vec{\pi}}{\kappa'}{\alpha}
}
{
   \totalisebwd{\sigma}{x \concat \vec{\pi}}{\elimVar{x}{\kappa'}}{\alpha}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-true}}
]
{
   \sigma \geq \elimBool{\kappa_1}{\annot{\exNil}{\alpha}}
   \\
   \totalisebwd{\kappa_1}{\vec{\pi}}{\kappa_1'}{\beta}
}
{
   \totalisebwd{\sigma}
               {\pattTrue \concat \vec{\pi}}
               {\elimBoolTrue{\kappa_1'}}
               {\alpha \join \beta}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-false}}
]
{
   \sigma \geq \elimBool{\annot{\exNil}{\alpha}}{\kappa_2}
   \\
   \totalisebwd{\kappa_2}{\vec{\pi}}{\kappa_2'}{\beta}
}
{
   \totalisebwd{\sigma}
               {\pattFalse \concat \vec{\pi}}
               {\elimBoolFalse{\kappa_2'}}
               {\alpha \join \beta}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-prod}}
]
{
   \sigma \geq \elimProd{\sigma'}
   \\
   \totalisebwd{\sigma'}{p \concat p' \concat \vec{\pi}}{\tau}{\beta}
}
{
   \totalisebwd{\sigma}{\pattPair{p}{p'} \concat \vec{\pi}}{\elimProd{\tau}}{\beta}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-nil}}
]
{
   \sigma \geq \elimList{\kappa}{\elimVar{x}{\elimVar{y}{\annot{\exNil}{\alpha}}}}
   \\
   \totalisebwd{\kappa}{\vec{\pi}}{\kappa'}{\beta}
}
{
   \totalisebwd{\sigma}
               {\pattNil \concat \vec{\pi}}
               {\elimNil{\kappa'}}
               {\alpha \join \beta}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-cons}}
]
{
   \sigma \geq \elimList{\annot{\exNil}{\alpha}}{\sigma'}
   \\
   \totalisebwd{\sigma'}{p \concat p' \concat \vec{\pi}}{\tau}{\beta}
}
{
   \totalisebwd{\sigma}
               {(\pattCons{p}{p'}) \concat \vec{\pi}}
               {\elimCons{\tau}}
               {\alpha \join \beta}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-non-empty-list}}
]
{
   \sigma \geq \elimList{\annot{\exNil}{\alpha}}{\sigma'}
   \\
   \totalisebwd{\sigma'}{p \concat o \concat \vec{\pi}}{\tau}{\beta}
}
{
   \totalisebwd{\sigma}
               {(\pattList{p}{o}) \concat \vec{\pi}}
               {\elimCons{\tau}}
               {\alpha \join \beta}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-list-rest-end}}
]
{
   \sigma \geq \elimList{\kappa}{\elimVar{x}{\elimVar{y}{\annot{\exNil}{\alpha}}}}
   \\
   \totalisebwd{\kappa}{\vec{\pi}}{\kappa'}{\beta}
}
{
   \totalisebwd{\sigma}
               {\pattListEnd \concat \vec{\pi}}
               {\elimNil{\kappa'}}
               {\alpha \join \beta}
}
%
\and
%
\inferrule*[
   lab={\ruleName{$\totalisebwdarrow$-list-rest-cons}}
]
{
   \sigma \geq \elimList{\annot{\exNil}{\alpha}}{\sigma'}
   \\
   \totalisebwd{\sigma'}{p \concat o \concat \vec{\pi}}{\tau}{\beta}
}
{
   \totalisebwd{\sigma}
               {(\pattListNext{p}{o}) \concat \vec{\pi}}
               {\elimCons{\tau}}
               {\alpha \join \beta}
}
\end{smathpar}
\caption{Totalise -- backward slicing}
\end{figure}
